export const validEmail = new RegExp(
  '/^[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}$/i'
  // '[A-Za-z]'
 );

 export const checkName = new RegExp(
    '[A-Za-z]'
  );

  export const quantityCheck = new RegExp (
    '[0-9]'
  );